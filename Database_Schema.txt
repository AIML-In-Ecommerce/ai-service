User {
	_id: ObjectId
	fullName: String
	dob: String
	avatar: String
	phoneNumber: String
	account: ObjectId

	address: [{
		_id: number
		receiverName: String
		address: String
		phoneNumber: String,
		
		coordinate:
		{
			lng: number (longitude)
			lat: number (latitude)
		},
		label: enum[HOME, OFFICE],
		isDefault: boolean
	}]
}

Shop {
	_id: ObjectId
	name: String
	location: String
	description: String
	...
}

Account {
	_id: ObjectId
	password: String
	email: String
	phoneNumber: String
	registerType: ObjectId
	type: ENUM[SHOP, BUYER, ADMIN]
	status: ENUM[ACTIVE, INACTIVE, BANED]
}

ChargeInformation {
	
}

Product {
	_id: ObjectId
	name String
	description: String
	originalPrice: Number
	finalPrice: Number
	category: ObjectId 
	subCategory: [ObjectId,...]
	shop: ObjectId
	status: ENUM[AVAILABLE, SOLD_OUT, SALE] 
	image: Array
	avgRating: Number
	createdAt: String
	attribute: [
		{
			attributeId:{
				_id:
				categoryId: {
				}
				name:
			}
			value: 
		},
		...
	]

	platformFee: Number
}

Category {
	_id:ObjectId 
	key: String
	urlKey: String
	name: String (ex: Laptop, Màn hình máy tính, Ổ cứng)
	image: String
	subCategoryType: [ObjectId,...]
}

SubCategoryType {		
	_id: ObjectId
	key: String
	urlKey: String
	attributeName: String (ex: displaySize)
	name: String (ex: Kích thước màn hình, Kích thước màn hình, Loại ổ cứng)
	subCategory: [ObjectId,...]
}

subCategory {
	_id: ObjectId
	key: String
	urlKey: String
	name: String (ex: 14 inch, 32inch, SSD)
	image: String
}


Promotion {
	_id: ObjectId
	name: String
	description: String
	discountType: ENUM [PERCENTAGE, DIRECT_PRICE]
	discountValue: Number
	quantity: Number
	activeDate: String
	expiredDate: String
	saleCategory: [ObjectId, ...]
	createedAt: String

	upperBound: number,
	lowerBound: number,
	code: String
	promotionType: ENUM [PRODUCT_PRICE, DELIVERY_FEE]
}

Cart {
	_id: ObjectId
	user: ObjectId
	product: [{
		productId: ObjectId
		quantity: Number
		},
		....]
	
}

Order {
	_id: ObjectId
	user: ObjectId
	product: [{
		productId: ObjectId,
		originPrice: number,
		purchasePrice: number,
		quantity: Number
	},
	...]
	promotion:ObjectId
	paymentMethod: ObjectId
	shippingFee: Number
	totalPrice: Number
	orderStatus: [{
		status: ENUM [PENDING, DELIVERING, SHIPPING, COMPLETED, CANCELLED]
		time: number/timestamp,
		deadline: number,

		complete: number - timestamp
	},
	...]

	profit: Number = totalPrice - TechZoneFee
	shippingAddress:
	{
		receiverName: String
		address: String
		phoneNumber: String,
		
		coordinate:
		{
			lng: number (longitude)
			lat: number (latitude)
		},
		label: enum[HOME, OFFICE],
		isDefault: boolean
	}
}

Review {
	_id: ObjectId
	product: ObjectId
	user: ObjectId
	rating: Number
	content: String
	asset: [String,...]
	createdAt: String
	conversation: [{
		user: ObjectId
		content: String,
		asset: String
	},
	...],
	like: [{
		user: ObjectId,
	}
	,...],
}


ProductAttribute {
	_id: ObjetcId
	name: String
}







